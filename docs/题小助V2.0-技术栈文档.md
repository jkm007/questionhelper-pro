# 题小助 V2.0 技术栈文档

**项目名称**：题小助 (QuestionHelper)
**版本**：V2.0
**日期**：2026-05-30

---

## 文档概述

本文档基于《题小助 V2.0 功能需求文档》梳理系统技术栈选型，涵盖后端、前端、数据库、缓存、消息队列、文件存储、部署架构及第三方服务。

---

## 目录

- [一、技术栈总览](#一技术栈总览)
- [二、后端技术栈](#二后端技术栈)
- [三、前端技术栈](#三前端技术栈)
- [四、数据库设计](#四数据库设计)
- [五、缓存设计](#五缓存设计)
- [六、消息队列](#六消息队列)
- [七、文件存储](#七文件存储)
- [八、第三方服务](#八第三方服务)
- [九、部署架构](#九部署架构)
- [十、开发工具与规范](#十开发工具与规范)
- [十一、WebSocket 实时通信](#十一websocket-实时通信)
- [十二、搜索方案](#十二搜索方案)
- [十三、安全方案](#十三安全方案)
- [十四、测试策略](#十四测试策略)
- [十五、运维监控](#十五运维监控)
- [十六、API 版本管理](#十六api-版本管理)

---

## 一、技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端语言 | Go | 高并发、高性能，适合教育平台场景 |
| Web 框架 | Gin | 轻量级、高性能，社区活跃 |
| ORM | GORM | Go 生态主流 ORM，支持关联、事务、迁移 |
| 数据库 | MySQL 8.0 | 关系型数据库，存储核心业务数据 |
| 缓存 | Redis 7.x | Token 管理、会话存储、延迟队列 |
| 消息队列 | Redis Stream / RabbitMQ | 考试超时交卷、通知推送 |
| 文件存储 | MinIO / 阿里云 OSS | 题目附件、头像、封面等文件存储 |
| 前端框架（管理后台） | vue3-element-admin (Vue 3 + Element Plus) | 管理后台 SPA，基于开源后台模板改造 |
| 前端框架（用户端） | uni-app | 一套代码多端运行（H5/小程序/App） |
| 对象存储 CDN | 阿里云 CDN / CloudFlare | 静态资源加速 |
| 容器化 | Docker + Docker Compose | 开发环境标准化 |
| CI/CD | GitHub Actions / GitLab CI | 自动化构建、测试、部署 |
| 反向代理 | Nginx | 负载均衡、SSL 终止、静态资源服务 |
| 日志 | ELK / Loki | 日志收集、查询、分析 |
| 监控 | Prometheus + Grafana | 系统监控、告警 |

---

## 二、后端技术栈

### 2.1 核心框架

| 组件 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| 编程语言 | Go | 1.21+ | 高并发、编译型语言 |
| Web 框架 | Gin | 1.9+ | HTTP 路由、中间件、参数绑定 |
| ORM | GORM | 1.25+ | 数据库操作、模型定义、自动迁移 |
| 参数校验 | go-playground/validator | v10+ | 请求参数结构体校验 |
| 日志 | Zap | 1.26+ | 高性能结构化日志 |
| 配置管理 | Viper | 1.16+ | 支持 YAML/TOML/JSON 配置文件 |
| API 文档 | Swagger (swaggo/swag) | - | 自动生成 OpenAPI 文档 |

### 2.2 认证与安全

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| Token 方案 | JWT (golang-jwt/jwt/v5) | Access Token + Refresh Token 双令牌 |
| 密码加密 | bcrypt (golang.org/x/crypto) | cost=12，安全强度高 |
| 图形验证码 | base64-captcha | 支持数字/字母/算术验证码 |
| CORS | gin-contrib/cors | 跨域资源共享配置 |
| 限流 | golang.org/x/time/rate | 令牌桶算法，API 限流 |
| SQL 注入防护 | GORM 参数化查询 | ORM 自动防注入 |
| XSS 防护 | bluemonday | HTML 内容消毒 |

### 2.3 业务组件

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| Excel 导入导出 | excelize/xlsx | 读写 Excel 文件 |
| Word 导出 | unioffice | 生成 Word 文档 |
| PDF 导出 | wkhtmltopdf / gofpdf | HTML/模板转 PDF |
| 文件上传 | multipart/form-data | 标准文件上传处理 |
| 富文本 | 数据库存储 HTML | 题目内容支持富文本 |
| 树形结构 | parent_id + path 方案 | 无限层级分类树 |
| 分页 | 自定义分页组件 | page + page_size + total |

---

## 三、前端技术栈

### 3.1 管理后台（Web）

**基础框架**：vue3-element-admin（基于 Vue 3 + Element Plus + Vite 的企业级后台模板）

| 组件 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| 基础模板 | vue3-element-admin | latest | 开箱即用的后台前端解决方案 |
| 框架 | Vue 3 | 3.4+ | Composition API、`<script setup>` 语法 |
| 构建工具 | Vite | 5.x | 快速开发构建、HMR 热更新 |
| UI 组件库 | Element Plus | 2.5+ | 企业级 UI 组件，按需引入 |
| 状态管理 | Pinia | 2.x | 轻量级状态管理，支持持久化 |
| 路由 | Vue Router | 4.x | 动态路由、路由守卫、权限控制 |
| HTTP 客户端 | Axios | 1.x | 请求拦截、响应拦截、Token 自动刷新 |
| 图表 | ECharts | 5.x | 数据可视化（趋势图、饼图、柱状图） |
| 富文本编辑器 | WangEditor | 5.x | 题目内容编辑，支持图片、公式 |
| 树形选择器 | Element Plus TreeSelect | - | 分类树选择、知识点选择 |
| 表格 | Element Plus Table + vxe-table | - | 复杂表格、虚拟滚动、大数据量 |
| 权限控制 | 路由权限 + 按钮权限指令 | - | 基于角色的菜单和按钮权限 |
| 国际化 | vue-i18n | 9.x | 多语言支持（可选） |
| 主题 | CSS 变量 + Element Plus 主题 | - | 亮色/暗色主题切换 |

**vue3-element-admin 内置功能**：

| 功能 | 说明 |
|------|------|
| 登录/注销 | 登录页面、Token 管理、退出登录 |
| 权限管理 | 动态路由、按钮权限指令 |
| 布局 | 侧边栏、顶部导航、面包签、标签页 |
| 请求封装 | Axios 封装、请求/响应拦截器 |
| 工具函数 | 日期处理、文件下载、数据转换 |
| 暗色主题 | 内置暗色模式切换 |
| 国际化 | 内置 i18n 方案 |
| SVG 图标 | 内置 SVG 图标组件 |

**改造要点**：

```
vue3-element-admin
├── src/
│   ├── api/              # 接口定义（对接后端 API）
│   ├── views/            # 页面开发（题库、考试、班级等模块）
│   ├── components/       # 自定义组件（树形选择器、富文本、试卷预览等）
│   ├── router/           # 路由配置（动态路由、权限路由）
│   ├── store/            # 状态管理（用户、权限、全局配置）
│   ├── hooks/            # 自定义 Hooks（分页、表格、表单等）
│   ├── utils/            # 工具函数（Token 刷新、文件上传等）
│   └── styles/           # 样式定制（主题色、品牌色）
├── .env.development      # 开发环境配置
├── .env.production       # 生产环境配置
└── vite.config.ts        # Vite 配置（代理、别名、插件）
```

### 3.2 动态菜单方案

> 菜单通过后端接口动态管理，前端根据用户角色动态加载

**技术实现**：

| 环节 | 方案 |
|------|------|
| 后端菜单管理 | GORM 模型 + 无限层级树（parent_id + path） |
| 菜单缓存 | Redis 缓存菜单数据，修改后自动刷新 |
| 前端菜单加载 | 登录后调用 `/api/user/menus` 获取菜单树 |
| 动态路由 | Vue Router 动态添加路由（router.addRoute） |
| 按钮权限 | v-permission 指令 + 权限标识控制 |

**前端动态菜单流程**：
```
用户登录 → 获取用户信息和角色 → 调用菜单接口 → 后端返回菜单树（含按钮权限）
    → 前端动态生成路由 → 渲染侧边栏菜单 → 存储按钮权限列表 → v-permission 控制按钮
```

**按钮权限实现**：

| 环节 | 方案 |
|------|------|
| 后端存储 | 菜单类型为 button，permission 字段存储权限标识 |
| 接口返回 | 菜单接口同时返回按钮权限列表（permission 标识数组） |
| 前端存储 | Pinia store 存储按钮权限列表 |
| 前端控制 | v-permission 指令判断按钮是否显示 |

**v-permission 指令实现**：
```typescript
// directive/permission.ts
export const vPermission = {
  mounted(el: HTMLElement, binding: DirectiveBinding) {
    const permissions = usePermissionStore().permissions
    const value = binding.value
    if (!permissions.includes(value)) {
      el.parentNode?.removeChild(el)
    }
  }
}
```

### 3.3 管理后台页面结构

基于 vue3-element-admin 改造，需要开发以下页面：

```
views/
├── dashboard/                    # 首页仪表盘
│   └── index.vue                 # 数据概览、趋势图表、快捷入口
├── user/                         # 用户管理
│   ├── list.vue                  # 用户列表
│   ├── detail.vue                # 用户详情
│   └── components/               # 用户相关组件
│       ├── RoleAssign.vue        # 角色分配弹窗
│       └── TagManage.vue         # 标签管理弹窗
├── role/                         # 角色管理
│   ├── list.vue                  # 角色列表
│   └── permission.vue            # 权限分配
├── permission/                   # 权限管理
│   └── tree.vue                  # 权限树管理
├── question/                     # 题库管理
│   ├── public.vue                # 公共题目列表
│   ├── private.vue               # 私有题目列表
│   ├── class.vue                 # 班级题目列表
│   ├── edit.vue                  # 题目编辑（新增/修改）
│   ├── import.vue                # 批量导入
│   ├── components/               # 题库相关组件
│       ├── QuestionForm.vue      # 题目表单
│       ├── CategoryTree.vue      # 分类树选择器
│       └── KnowledgeSelect.vue   # 知识点选择器
│   ├── category/                 # 分类管理
│   │   └── tree.vue              # 分类树管理
│   └── knowledge/                # 知识点管理
│       └── list.vue              # 知识点列表
├── exam/                         # 考试管理
│   ├── paper/                    # 试卷管理
│   │   ├── list.vue              # 试卷列表
│   │   ├── manual.vue            # 手动组卷
│   │   └── smart.vue             # 智能组卷
│   ├── publish/                  # 考试发布
│   │   ├── list.vue              # 考试列表
│   │   ├── create.vue            # 创建考试
│   │   └── monitor.vue           # 考试监控
│   ├── score/                    # 成绩管理
│   │   ├── list.vue              # 成绩列表
│   │   ├── review.vue            # 主观题阅卷
│   │   └── analysis.vue          # 成绩分析
│   └── components/               # 考试相关组件
│       ├── QuestionPicker.vue    # 题目选择器
│       └── ExamPreview.vue       # 试卷预览
├── class/                        # 班级管理
│   ├── list.vue                  # 班级列表
│   ├── detail.vue                # 班级详情
│   ├── member.vue                # 成员管理
│   ├── creator.vue               # 班级创作者管理
│   ├── resource.vue              # 班级资源管理
│   └── components/               # 班级相关组件
│       ├── InviteCode.vue        # 邀请码组件
│       └── MemberImport.vue      # 成员导入
├── comment/                      # 评论管理
│   ├── list.vue                  # 评论列表
│   └── report.vue                # 举报管理
├── statistics/                   # 数据统计
│   ├── overview.vue              # 数据概览
│   ├── user.vue                  # 用户统计
│   ├── question.vue              # 题目统计
│   ├── exam.vue                  # 考试统计
│   └── class.vue                 # 班级统计
├── system/                       # 系统设置
│   ├── settings.vue              # 基础设置
│   ├── log/                      # 日志管理
│   │   ├── operation.vue         # 操作日志
│   │   └── login.vue             # 登录日志
│   ├── notification.vue          # 通知设置
│   └── backup.vue                # 数据备份
├── file/                         # 文件管理
│   └── list.vue                  # 文件列表
└── profile/                      # 个人中心
    ├── index.vue                 # 个人信息
    ├── password.vue              # 修改密码
    └── message.vue               # 我的消息
```

### 3.3 用户端（H5/小程序）

| 组件 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| 框架 | uni-app | 3.x | 一套代码多端运行 |
| UI 组件库 | uView Plus | 2.x | uni-app 生态 UI 库 |
| 状态管理 | Pinia | 2.x | 跨端状态管理 |
| HTTP 客户端 | uni.request 封装 | - | 统一请求拦截 |
| 路由拦截 | uni.addInterceptor | - | 页面权限拦截 |
| 本地存储 | uni.setStorageSync | - | Token 缓存 |
| 分享 | uni.share | - | 微信/小程序分享 |

### 3.4 前端公共模块

| 模块 | 说明 |
|------|------|
| Token 自动刷新 | Axios 拦截器，401 时自动刷新 Token |
| 请求队列 | Token 刷新期间请求排队，刷新完成后重发 |
| 权限路由 | 根据用户角色动态生成路由表 |
| 错误处理 | 统一错误码处理、友好提示 |
| 国际化 | vue-i18n（可选） |
| 主题切换 | CSS 变量 + 暗色模式（可选） |

---

## 四、数据库设计

### 4.1 数据库选型

| 数据库 | 用途 | 说明 |
|-------|------|------|
| MySQL 8.0 | 主数据库 | 存储用户、题目、考试、班级等核心数据 |
| Redis 7.x | 缓存/会话 | Token 黑名单、考试会话、延迟队列 |

### 4.2 核心数据表

| 模块 | 表名 | 说明 |
|------|------|------|
| 用户 | users | 用户基本信息 |
| 用户 | user_profiles | 用户详细资料 |
| 用户 | user_roles | 用户角色关联 |
| 用户 | user_tags | 用户标签关联 |
| 用户 | user_third_party | 第三方账号绑定 |
| 用户 | user_real_name | 实名认证信息 |
| 用户 | role_apply | 角色申请记录 |
| 权限 | roles | 角色表 |
| 权限 | permissions | 权限表 |
| 权限 | role_permissions | 角色权限关联 |
| 题库 | questions | 题目表 |
| 题库 | categories | 分类表（无限层级） |
| 题库 | knowledge_points | 知识点表 |
| 题库 | question_knowledge | 题目知识点关联 |
| 题库 | question_snapshots | 题目快照 |
| 题库 | question_versions | 题目版本历史 |
| 考试 | exams | 试卷表 |
| 考试 | exam_rules | 组卷规则 |
| 考试 | exam_questions | 试卷题目关联 |
| 考试 | exam_publish | 考试发布记录 |
| 考试 | exam_records | 答题记录 |
| 考试 | exam_answers | 答题详情 |
| 班级 | classes | 班级表 |
| 班级 | class_members | 班级成员 |
| 班级 | class_creator_apply | 班级创作者申请 |
| 练习 | practice_sessions | 练习会话 |
| 练习 | practice_records | 练习记录 |
| 错题 | wrong_questions | 错题本 |
| 评论 | comments | 评论表 |
| 评论 | comment_likes | 评论点赞 |
| 通知 | notifications | 通知表 |
| 系统 | audit_logs | 审计日志 |
| 系统 | system_settings | 系统配置 |
| 文件 | files | 文件记录 |

### 4.3 索引设计原则

- 主键：统一使用 `id BIGINT UNSIGNED AUTO_INCREMENT`
- 外键：应用层维护，不使用数据库外键约束
- 唯一索引：用户名、邮箱、手机号等业务唯一字段
- 普通索引：高频查询字段（status、category_id、created_at 等）
- 复合索引：多条件组合查询（如 user_id + status）
- 软删除：使用 `deleted_at DATETIME NULL` 字段

### 4.4 数据库配置

```yaml
# MySQL 配置
mysql:
  host: localhost
  port: 3306
  database: questionhelper
  charset: utf8mb4
  collation: utf8mb4_unicode_ci
  max_open_conns: 100
  max_idle_conns: 10
  conn_max_lifetime: 3600  # 秒
  timezone: Asia/Shanghai

# 备份策略
backup:
  strategy: daily_full + binlog_incremental
  retention_days: 30
  schedule: "0 2 * * *"  # 每日凌晨 2 点
```

---

## 五、缓存设计

### 5.1 Redis 使用场景

| 场景 | Key 格式 | 数据结构 | TTL | 说明 |
|------|---------|---------|-----|------|
| Token 黑名单 | `qh:token:bl:{jti}` | String | Token 剩余有效期 | 退出登录后 Token 失效 |
| 考试会话 | `qh:exam:session:{uid}:{eid}` | Hash | 考试时长 | 考试状态、题目顺序 |
| 用户答案 | `qh:exam:answers:{uid}:{eid}` | Hash | 考试时长 | 用户答题缓存 |
| 考试分值 | `qh:exam:scores:{uid}:{eid}` | Hash | 考试时长 | 每题分值 |
| 考试延迟队列 | `qh:exam:delay` | ZSet | - | Score=交卷时间戳 |
| 图形验证码 | `qh:captcha:{key}` | String | 5 分钟 | 验证码存储 |
| 短信验证码 | `qh:sms:{phone}` | String | 5 分钟 | 验证码存储 |
| 短信频率限制 | `qh:sms:limit:{phone}` | String | 60 秒 | 防止频繁发送 |
| 每日短信限制 | `qh:sms:daily:{phone}` | String | 24 小时 | 每日上限 |
| IP 登录限制 | `qh:login:limit:{ip}` | String | 1 小时 | IP 级别限流 |
| 用户登录失败 | `qh:login:fail:{uid}` | String | 15 分钟 | 账号锁定 |
| 热门题目 | `qh:hot:questions` | ZSet | 1 小时 | 热度排行 |
| 用户会话 | `qh:user:session:{uid}` | Hash | 7 天 | 用户在线状态 |

### 5.2 Redis 配置

```yaml
redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  max_retries: 3
  pool_size: 100
  min_idle_conns: 10
  dial_timeout: 5s
  read_timeout: 3s
  write_timeout: 3s
```

---

## 六、消息队列

### 6.1 使用场景

| 场景 | 说明 | 优先级 |
|------|------|-------|
| 考试超时交卷 | 延迟队列，考试结束自动交卷 | 高 |
| 通知推送 | 异步发送站内消息、短信、邮件 | 中 |
| 数据统计 | 异步更新统计数据 | 低 |
| 文件处理 | 异步处理图片压缩、缩略图生成 | 低 |

### 6.2 方案选型

| 方案 | 适用场景 | 说明 |
|------|---------|------|
| Redis ZSet | 延迟队列（考试超时） | 轻量级，适合简单延迟任务 |
| Redis Stream | 消息队列（通知推送） | Redis 5.0+ 原生支持 |
| RabbitMQ | 复杂消息路由 | 功能丰富，支持多种模式 |

**建议**：
- 考试超时交卷：使用 Redis ZSet 延迟队列（已在需求文档中定义）
- 通知推送：使用 Redis Stream 或 Go Channel
- 如需更复杂的消息路由，可引入 RabbitMQ

---

## 七、文件存储

### 7.1 存储方案

| 方案 | 适用场景 | 说明 |
|------|---------|------|
| 本地存储 | 开发环境 | 简单直接，无需额外服务 |
| MinIO | 私有化部署 | 兼容 S3 协议，自建对象存储 |
| 阿里云 OSS | 生产环境 | 云服务，高可用、高可靠 |
| 腾讯云 COS | 生产环境 | 云服务，与微信生态兼容 |

### 7.2 目录结构

```
/ (bucket root)
├── avatar/          # 用户头像
├── question/        # 题目附件（图片、音频、视频）
├── exam/            # 试卷导出文件
├── class/           # 班级封面、班级文件
├── temp/            # 临时文件（24小时后清理）
└── export/          # 数据导出文件（Excel、PDF）
```

### 7.3 CDN 配置

```yaml
cdn:
  enabled: true
  domain: cdn.questionhelper.com
  # 图片处理（阿里云 OSS 图片处理）
  image_style:
    thumbnail: "?x-oss-process=image/resize,m_fill,w_100,h_100"
    medium: "?x-oss-process=image/resize,m_fill,w_300,h_300"
    large: "?x-oss-process=image/resize,m_fill,w_800,h_800"
```

---

## 八、第三方服务

### 8.1 短信服务

| 服务商 | 说明 | 适用场景 |
|-------|------|---------|
| 阿里云短信 | 国内主流，到达率高 | 生产环境 |
| 腾讯云短信 | 与微信生态兼容 | 生产环境 |
| Twilio | 国际短信 | 海外用户 |

### 8.2 邮件服务

| 服务商 | 说明 | 适用场景 |
|-------|------|---------|
| 阿里云邮件推送 | 国内邮件服务 | 生产环境 |
| SendGrid | 国际邮件服务 | 海外用户 |
| SMTP 自建 | 自建邮件服务器 | 私有化部署 |

### 8.3 微信生态

| 服务 | 说明 |
|------|------|
| 微信小程序登录 | wx.login() + code2session |
| 微信公众号登录 | OAuth2.0 网页授权 |
| 微信扫码登录 | PC 端扫码授权 |
| 微信支付 | 付费功能（如有） |
| 微信模板消息 | 小程序消息推送 |
| 微信公众号推送 | 公众号模板消息/订阅消息 |

### 8.4 第三方 OAuth

| 平台 | 说明 |
|------|------|
| GitHub | OAuth2.0 授权登录 |
| Google | OAuth2.0 授权登录 |

---

## 九、部署架构

### 9.1 开发环境

```
┌─────────────────────────────────────────┐
│              Docker Compose             │
├─────────┬─────────┬─────────┬───────────┤
│  MySQL  │  Redis  │  MinIO  │   App     │
│  8.0    │  7.x    │  latest │   Go+Vue  │
│  :3306  │  :6379  │  :9000  │   :8080   │
└─────────┴─────────┴─────────┴───────────┘
```

### 9.2 生产环境

```
                    ┌─────────────┐
                    │    CDN      │
                    │  (静态资源)  │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │    Nginx    │
                    │  (反向代理)  │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
     ┌──────▼──────┐ ┌────▼────┐  ┌──────▼──────┐
     │   App-1     │ │  App-2  │  │   App-3     │
     │  (Go API)   │ │ (Go API)│  │  (Go API)   │
     └──────┬──────┘ └────┬────┘  └──────┬──────┘
            │              │              │
            └──────────────┼──────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
  ┌──────▼──────┐   ┌─────▼─────┐   ┌──────▼──────┐
  │   MySQL     │   │   Redis   │   │   MinIO     │
  │  (主从复制)  │   │  (哨兵)   │   │  (集群)     │
  └─────────────┘   └───────────┘   └─────────────┘
```

### 9.3 部署方式

| 方式 | 适用场景 | 说明 |
|------|---------|------|
| Docker Compose | 小规模部署 | 单机部署，简单快速 |
| Docker Swarm | 中等规模 | 多机集群，易管理 |
| Kubernetes | 大规模部署 | 容器编排，高可用 |

---

## 十、开发工具与规范

### 10.1 开发工具

| 工具 | 用途 |
|------|------|
| GoLand / VS Code | Go 开发 IDE |
| WebStorm / VS Code | 前端开发 IDE |
| Navicat / DBeaver | 数据库管理 |
| Postman / Apifox | API 调试 |
| Redis Desktop Manager | Redis 管理 |
| Git | 版本控制 |

### 10.2 代码规范

| 规范 | 说明 |
|------|------|
| Go | 遵循 Go 官方规范，使用 golangci-lint |
| Vue | 遵循 Vue 官方规范，使用 ESLint + Prettier |
| Git | Conventional Commits 规范 |
| API | RESTful 风格，统一响应格式 |
| 错误码 | 统一错误码体系（模块+错误类型+序号） |

### 10.3 API 响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": 1717036800
}
```

**错误码规范**：
- `0`：成功
- `1xxx`：参数错误
- `2xxx`：认证/授权错误
- `3xxx`：业务逻辑错误
- `5xxx`：服务器内部错误

---

## 十一、WebSocket 实时通信

### 11.1 使用场景

| 场景 | 说明 |
|------|------|
| 考试监控 | 教师端实时查看考试状态（答题人数、提交人数） |
| 站内消息 | 通知实时推送到前端（评论回复、考试评分、系统公告） |
| 在线状态 | 用户在线/离线状态实时更新 |

### 11.2 技术选型

| 方案 | 说明 | 适用场景 |
|------|------|---------|
| Gorilla WebSocket | Go 生态主流 WebSocket 库 | 轻量级实时通信 |
| Socket.IO | 支持降级、房间、命名空间 | 需要兼容性保障 |
| Redis Pub/Sub | 消息广播，支持多实例部署 | 多服务器消息同步 |

**建议**：使用 Gorilla WebSocket + Redis Pub/Sub，支持水平扩展。

---

## 十二、搜索方案

### 12.1 使用场景

| 场景 | 说明 |
|------|------|
| 题目搜索 | 按关键词搜索题目内容、标题 |
| 用户搜索 | 按用户名、邮箱、手机号搜索 |
| 评论搜索 | 搜索评论内容 |
| 分类搜索 | 搜索分类名称 |

### 12.2 技术选型

| 方案 | 说明 | 适用场景 |
|------|------|---------|
| MySQL LIKE | 简单模糊查询 | 数据量小、查询简单 |
| MySQL 全文索引 | FULLTEXT INDEX | 中等数据量、中文分词 |
| Elasticsearch | 分布式搜索引擎 | 大数据量、复杂搜索、高并发 |
| Meilisearch | 轻量级搜索引擎 | 中小规模、部署简单 |

**建议**：
- 初期：MySQL LIKE + 索引优化（数据量 < 100 万）
- 中期：MySQL 全文索引 + 中文分词插件
- 后期：Elasticsearch / Meilisearch（数据量大或搜索复杂度高时）

---

## 十三、安全方案

### 13.1 接口安全

| 措施 | 技术方案 |
|------|---------|
| 认证 | JWT + Redis Token 黑名单 |
| 授权 | 自定义 RBAC 权限中间件 |
| 限流 | golang.org/x/time/rate（令牌桶） |
| 防重放 | 请求签名 + 时间戳 + Nonce |
| CORS | gin-contrib/cors 白名单配置 |

**接口权限实现**：

| 环节 | 方案 |
|------|------|
| 权限标识 | 路由定义时绑定 permission 标识（如 `user:view`） |
| 中间件校验 | 请求到达时，中间件从 Token 解析用户角色，查询角色权限列表 |
| 权限匹配 | 判断用户权限列表是否包含当前接口的 permission 标识 |
| 缓存优化 | 用户权限列表缓存到 Redis，避免每次请求查库 |

**接口权限中间件流程**：
```
请求到达 → JWT 中间件解析 Token → 获取用户 ID
    → 从 Redis 缓存读取用户权限列表
    → 匹配当前接口的 permission 标识
    → 有权限 → 继续处理
    → 无权限 → 返回 403 Forbidden
```

### 13.2 数据权限

| 场景 | 方案 |
|------|------|
| 公共资源 | 所有用户可见 |
| 私有资源 | 仅创建者可见（WHERE created_by = ?） |
| 班级资源 | 仅班级成员可见（WHERE class_id IN 用户加入的班级） |
| 管理员 | 可查看所有数据 |

**数据权限实现**：
- Repository 层根据用户角色和资源类型自动拼接查询条件
- 公共资源：无条件限制
- 私有资源：`AND created_by = ?`
- 班级资源：`AND class_id IN (?)`

### 13.3 资源权限

| 资源类型 | 可见范围 | 创建权限 |
|---------|---------|---------|
| 公共题目 | 所有用户 | 内容管理员/管理员 |
| 私有题目 | 仅创建者 | 创作者/教师 |
| 班级题目 | 班级成员 | 教师/班级创作者 |

**资源权限实现**：
- Service 层根据用户角色判断是否有创建权限
- Repository 层根据资源类型和用户信息过滤数据
- Controller 层返回数据时自动过滤无权限数据

### 13.2 数据安全

| 措施 | 技术方案 |
|------|---------|
| 密码加密 | bcrypt（cost=12） |
| SQL 注入 | GORM 参数化查询 |
| XSS 防护 | bluemonday HTML 消毒 |
| CSRF 防护 | SameSite Cookie + Token 验证 |
| 敏感数据 | 手机号、身份证号脱敏存储/展示 |

### 13.3 内容安全

| 措施 | 技术方案 |
|------|---------|
| 敏感词过滤 | go-sensitive / 自定义敏感词库（DFA 算法） |
| 图片审核 | 阿里云内容安全 / 腾讯云天御 |
| 文本审核 | 阿里云内容安全 / 自建关键词库 |

### 13.4 防作弊技术

| 措施 | 实现方式 |
|------|---------|
| 题目乱序 | 服务端生成随机顺序，存入 Redis |
| 选项乱序 | 服务端生成随机顺序，存入 Redis |
| 服务端倒计时 | Redis TTL + 延迟队列，不依赖客户端时间 |
| 切屏检测 | Page Visibility API + 前端埋点上报 |
| 答题行为分析 | 记录答题时间、切换次数等异常指标 |

---

## 十四、测试策略

### 14.1 后端测试

| 测试类型 | 工具 | 说明 |
|---------|------|------|
| 单元测试 | testing + testify | 业务逻辑、工具函数 |
| 集成测试 | testing + httptest | API 接口测试 |
| 数据库测试 | testing + GORM | 数据库操作测试 |
| Mock | gomock / testify/mock | 依赖服务 Mock |
| 覆盖率 | go test -cover | 目标覆盖率 > 70% |

### 14.2 前端测试

| 测试类型 | 工具 | 说明 |
|---------|------|------|
| 单元测试 | Vitest | 组件、工具函数 |
| 组件测试 | Vue Test Utils | Vue 组件渲染测试 |
| E2E 测试 | Playwright / Cypress | 关键流程端到端测试 |
| 覆盖率 | Vitest coverage | 目标覆盖率 > 60% |

### 14.3 性能测试

| 工具 | 用途 |
|------|------|
| k6 | 负载测试、压力测试 |
| wrk | HTTP 基准测试 |
| Apache Bench (ab) | 简单压测 |
| pprof | Go 性能分析 |

---

## 十五、运维监控

### 15.1 监控方案

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 指标采集 | Prometheus | 系统指标、业务指标采集 |
| 可视化 | Grafana | 监控仪表盘、图表展示 |
| 告警 | Alertmanager | 告警规则、通知渠道 |
| 日志 | Loki + Promtail | 轻量级日志收集 |
| 链路追踪 | Jaeger / OpenTelemetry | 分布式链路追踪（可选） |

### 15.2 监控指标

| 类型 | 指标 |
|------|------|
| 系统指标 | CPU、内存、磁盘、网络 |
| 应用指标 | QPS、响应时间、错误率、并发数 |
| 数据库指标 | 连接数、慢查询、QPS |
| Redis 指标 | 内存使用、命中率、连接数 |
| 业务指标 | 在线用户数、考试进行数、注册数 |

### 15.3 告警规则

| 场景 | 告警条件 |
|------|---------|
| 服务宕机 | 实例不可用 > 1 分钟 |
| 高延迟 | P99 响应时间 > 3 秒 |
| 高错误率 | 5xx 错误率 > 1% |
| 磁盘空间 | 磁盘使用率 > 85% |
| 内存使用 | 内存使用率 > 90% |
| 慢查询 | 慢查询数量 > 10/分钟 |

---

## 十六、API 版本管理

### 16.1 版本策略

| 方案 | 说明 | 适用场景 |
|------|------|---------|
| URL 路径版本 | `/api/v1/users` | 推荐，清晰直观 |
| Header 版本 | `Accept: application/vnd.api.v1+json` | RESTful 风格 |
| 查询参数 | `/api/users?version=1` | 简单但不推荐 |

**建议**：使用 URL 路径版本，如 `/api/v1/`。

### 16.2 版本兼容

- 新版本发布后，旧版本保留至少 6 个月
- 版本变更通过 API 文档和变更日志通知
- 重大变更（Breaking Change）必须升级主版本号

---

**文档结束**

**文档结束**
